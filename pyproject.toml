[project]
name = "fdtd-jax"
version = "0.1.0"
description = "A simple FDTD implementation in JAX"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
